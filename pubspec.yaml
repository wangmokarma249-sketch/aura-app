name: aura
description: A modern health and fitness companion app with a calm, minimal lavender theme
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  supabase_flutter: ^2.3.4
  fl_chart: ^0.65.0
  shared_preferences: ^2.2.2
  provider: ^6.1.1
  intl: ^0.18.1
  pedometer: ^4.0.2
  permission_handler: ^11.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  vibration: ^2.0.0
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
